
# Workflow derived from https://github.com/r-lib/actions/tree/v2/examples
# Need help debugging build failures? Start at https://github.com/r-lib/actions#where-to-find-help

name: r-cmd-check
on: {
  push: {branches: [main, master]},
  pull_request: {branches: [main, master]}
}

jobs:
  r-cmd-check:
    runs-on: ${{ matrix.config.os }}
    name: ${{ matrix.config.os }} (${{ matrix.config.r }})
    strategy:
      fail-fast: false
      matrix:
        config:
          - {os: macos-latest,    r: 'release'}
          - {os: windows-latest,  r: 'release'}
          - {os: ubuntu-latest,   r: 'devel', http-user-agent: 'release'}
          - {os: ubuntu-latest,   r: 'release'}
          - {os: ubuntu-latest,   r: 'oldrel-1'}
          - {os: ubuntu-latest,   r: '4.1'}
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      R_KEEP_PKG_SOURCE: yes
    steps:

      - name: Checkout repository
        uses: actions/checkout@v4

      # - name: Set up Pandoc
      #   uses: r-lib/actions/setup-pandoc@v2

      - name: Set up R
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: ${{ matrix.config.r }}
          http-user-agent: ${{ matrix.config.http-user-agent }}
          use-public-rspm: true

      # - name: Install R
      #   run: sudo bash misc/scripts/install-r.sh

      - name: Set up R dependencies
        uses: r-lib/actions/setup-r-dependencies@v2
        with: { extra-packages: "any::rcmdcheck", needs: "check" }

      # - name: Install system packages
      #   run: sudo bash misc/scripts/install-sys-pkgs.sh

      # - name: Install R dev packages
      #   run: sudo Rscript misc/scripts/install-r-dev-pkgs.R

      # - name: Run R CMD javareconf
      #   run: sudo R CMD javareconf

      # - name: Install R Java
      #   run: sudo Rscript misc/scripts/install-rjava.R

      # - name: Install rCDK
      #   run: sudo Rscript misc/scripts/install-rcdk.R --verbose

      # - name: Test rCDK
      #   run: sudo Rscript misc/scripts/test-rcdk.R --progress --descriptors 'c(1:19, 21:53)'

      # - name: Install FastRet
      #   run: sudo Rscript misc/scripts/install-fastret.R --branch fix-actions --verbose

      - name: Test FastRet
        uses: r-lib/actions/check-r-package@v2
